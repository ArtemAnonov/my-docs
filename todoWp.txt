**************************************************************
+- Оформление заказа незарег. пользователю.

	--20%
Не получается переопределить маршрут. Для определения функции, которая должна возвращать response, нужен объект Checkot, который используется в RoutesController. 
	- для его создания нужны объекты, которые создаются вместе с RoutesController в хуке 'rest_api_init', в нем же необходимо создавать/переопределять маршрут (мне нуэно переопределить колбэк разрешения).

Оказалось передавал не тот объект
	--------80%
Осталось загадкой то, как хендлер хука(?) обрабатывает очередь. В моём случае получилось получить цепочкой get-методов нужный объект

Как оказалось, некоторые аргументы 3 параметра register_rest_route() для Checkout, которые используются в методе имеют protected доступ.

***
		Попытка отменить регистрацию дефолтныйх 				классов с заменой на 	новые.
Функция remove_action() работает с добавлением имени хука и функции, которая соответсвует экшену.
В WC, например в Woocommerce классе добавление экшена загрузки rest_api происходит с добавлением функции, на которую можно сослаться - она присутсвует в классе с доступом public.
В WCGB в классе StoreApi функция коллбэк - это анонимная функция, к которой я пока не придумал доступа...

Класс StoreApi используется многими другими классами. Так как в нем имеется контейнер для объектов. Для замены Chechout необходимо не только, отключив действие, расширить StoreApi и пару классов (как в WC), но и переписать множество других классов, заменяя в них StoreApi на модифицированный класс.

Для проверки has_action() нужен тот объект, для которого вызывалась функция


**************************************************************
+- Сохранение текущей корзины для вновь зарегистрированного пользователя


**************************************************************

+- Ошибка с сеансом/сессией - кешируется корзина гостя после авторизации пользователя и запроса его корзины


**************************************************************
+- Передача в оболочку rest woo для web только тех параметров, которые доступны в модели BasedModels, то есть для дочернего класс
///
В WCGB есть возможность их получения


**************************************************************
- Обновления токена...


**************************************************************
+- Проверка csrf (например из форм фронтенда)